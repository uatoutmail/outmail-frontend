// Tailwind CSS v4 PostCSS config. All design tokens live in src/app/globals.css
// (@theme). There is no JS theme config in v4 — do not add `content`/`theme` here.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
